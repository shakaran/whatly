name: Unit Tests

permissions:
  contents: read

# Builds and runs the QtTest suite (headless) on every push and PR, so an icon,
# logo or resource regression like the blank About buttons is caught in CI.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          submodules: recursive

      - name: Install Qt
        uses: jurplel/install-qt-action@48d3ad6db93f3627c8ee7a0454bc6f3744f7e730 # v4
        with:
          version: '6.10.3'  # exact pin (#71): bump deliberately, don't glob
          host: linux
          target: desktop
          arch: linux_gcc_64
          modules: 'qtwebengine qtwebchannel qtpositioning'
          cache: true

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y ninja-build libx11-dev libxcb1-dev

      - name: Configure (tests on)
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug -DWHATLY_TESTS=ON -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR"

      - name: Build the tests
        run: cmake --build build --target tst_ui_assets tst_logic

      # tst_settings (the whole-app SettingsWidget coverage test) is a local-only
      # tool: it passes locally but the GitHub runner's headless environment
      # aborts it early for reasons that don't reproduce off-CI, so it is not run
      # here. The robust unit suites below are the CI regression guard.
      - name: Run the tests (headless)
        env:
          QT_QPA_PLATFORM: offscreen
        run: ctest --test-dir build --output-on-failure -E settings
